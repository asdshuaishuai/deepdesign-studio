{
  "name": "agent",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "esbuild agent-bridge.mjs --bundle --platform=node --format=esm --outfile=agent-bridge.bundle.mjs --sourcemap --legal-comments=none"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@open-agent-loops/core": "^1.1.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}